<%= form_for(@invitation, :html => { :class => "form-inline" }) do |f| %>

    <!-- < %= f.text_field :email, :placeholder => "Email", :class => 'input-small' %> -->
    <%= f.text_field :email, :placeholder => "Email", :class => 'input-medium' %>

    <%= f.button "Invite", :class => "btn btn-small btn-primary" %>

<% end %>
